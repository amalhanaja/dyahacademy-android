# Write your query or mutation here
query GetLessonById($id: ID!){
    lesson(id: $id) {
        id
        title
        markdown
        youtubeUrl
        lessonType
        quizzes {
            id
            question
            answers
        }
    }
}