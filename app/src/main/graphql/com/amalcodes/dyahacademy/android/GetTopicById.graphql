query GetTopicById($id: ID!){
    topic(id: $id) {
        id
        title
        lessons {
            title
            id
            youtubeUrl
            document {
                url
                ext
            }
            markdown
            lessonType
        }
    }
}