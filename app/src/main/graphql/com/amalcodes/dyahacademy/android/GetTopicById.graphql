query GetTopicById($id: ID!){
    topic(id: $id) {
        id
        title
        lessons {
            title
            id
            lessonType
            lessons {
                title
                id
                lessonType
            }
        }
    }
}